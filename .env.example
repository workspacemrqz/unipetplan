# ========================================
# UNIPET PLAN - Variáveis de Ambiente
# ========================================

# ========================================
# AUTENTICAÇÃO ADMIN (OBRIGATÓRIO)
# ========================================
# Credenciais de administrador
# IMPORTANTE: SENHA deve ser um hash bcrypt (use bcrypt online ou bcrypt.hash())
LOGIN=admin@unipetplan.com.br
SENHA=$2b$12$EXEMPLO_DE_HASH_BCRYPT_AQUI

# ========================================
# SEGURANÇA (OBRIGATÓRIO EM PRODUÇÃO)
# ========================================
# Chave secreta para sessões - CRÍTICO em produção
# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# NUNCA compartilhe esta chave ou commite no git!
SESSION_SECRET=GERE_UMA_CHAVE_ALEATORIA_DE_64_CARACTERES_HEX

# ========================================
# BANCO DE DADOS
# ========================================
# PostgreSQL connection string
# Formato: postgresql://usuario:senha@host:porta/database
DATABASE_URL=postgresql://user:password@localhost:5432/unipetplan

# ========================================
# AMBIENTE
# ========================================
# Ambiente de execução (development | production)
NODE_ENV=development

# Porta do servidor (padrão: 3000)
PORT=3000

# Host do servidor (padrão: 0.0.0.0)
HOST=0.0.0.0

# ========================================
# ARMAZENAMENTO DE IMAGENS
# ========================================
# Tipo de storage: local | google-cloud
STORAGE_TYPE=local

# Google Cloud Storage (opcional, apenas se STORAGE_TYPE=google-cloud)
# GOOGLE_CLOUD_PROJECT_ID=seu-projeto-id
# PRIVATE_OBJECT_DIR=caminho/do/storage

# ========================================
# GATEWAY DE PAGAMENTO - CIELO
# ========================================
# Credenciais Cielo E-commerce
CIELO_MERCHANT_ID=seu-merchant-id
CIELO_MERCHANT_KEY=sua-merchant-key

# ========================================
# EMAIL (NODEMAILER)
# ========================================
# Configurações de email para envio
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=seu-email@gmail.com
EMAIL_PASS=sua-senha-de-app

# ========================================
# DOMÍNIOS (PRODUÇÃO)
# ========================================
# Domínio principal da aplicação
DOMAIN=https://unipetplan.com.br

# ========================================
# OBSERVAÇÕES DE SEGURANÇA
# ========================================
# 1. NUNCA commite o arquivo .env no git
# 2. Adicione .env ao .gitignore
# 3. Use senhas bcrypt (nunca texto plano)
# 4. SESSION_SECRET é OBRIGATÓRIO em produção
# 5. Rotacione secrets regularmente
# 6. Use variáveis de ambiente do host em produção
